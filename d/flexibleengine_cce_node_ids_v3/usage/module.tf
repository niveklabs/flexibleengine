module "flexibleengine_cce_node_ids_v3" {
  source = "./modules/flexibleengine/d/flexibleengine_cce_node_ids_v3"

  # cluster_id - (required) is a type of string
  cluster_id = null
  # region - (optional) is a type of string
  region = null
}
