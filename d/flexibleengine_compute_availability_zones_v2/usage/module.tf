module "flexibleengine_compute_availability_zones_v2" {
  source = "./modules/flexibleengine/d/flexibleengine_compute_availability_zones_v2"

  # region - (optional) is a type of string
  region = null
  # state - (optional) is a type of string
  state = null
}
