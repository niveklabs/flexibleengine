module "flexibleengine_rts_stack_v1" {
  source = "./modules/flexibleengine/d/flexibleengine_rts_stack_v1"

  # name - (required) is a type of string
  name = null
  # region - (optional) is a type of string
  region = null
}
