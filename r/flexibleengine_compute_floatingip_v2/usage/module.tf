module "flexibleengine_compute_floatingip_v2" {
  source = "./modules/flexibleengine/r/flexibleengine_compute_floatingip_v2"

  # pool - (optional) is a type of string
  pool = null
  # region - (optional) is a type of string
  region = null
}
